@php($editing = isset($task))
<form method="POST" action="{{ $editing ? route('tasks.update', $task) : route('tasks.store', $project) }}" class="space-y-5">@csrf @if($editing) @method('PUT') @endif
    <label class="block text-sm font-semibold">Task title<input name="title" required value="{{ old('title', $task->title ?? '') }}" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3 @error('title') border-rose-400 @enderror"></label>@error('title')<p class="text-sm text-rose-600">{{ $message }}</p>@enderror
    <label class="block text-sm font-semibold">Description<textarea name="description" rows="6" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3">{{ old('description', $task->description ?? '') }}</textarea></label>
    <div class="grid gap-5 sm:grid-cols-2"><label class="block text-sm font-semibold">Priority<select name="priority" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3">@foreach($priorities as $priority)<option value="{{ $priority->value }}" @selected(old('priority', $task->priority->value ?? 'medium') === $priority->value)>{{ ucfirst($priority->value) }}</option>@endforeach</select></label><label class="block text-sm font-semibold">Due date<input type="date" name="due_at" value="{{ old('due_at', isset($task) && $task->due_at ? $task->due_at->format('Y-m-d') : '') }}" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3"></label></div>
    @unless($editing)<label class="block text-sm font-semibold">Assignee<select name="assignee_id" class="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3"><option value="">Unassigned</option>@foreach($memberships as $membership)<option value="{{ $membership->user_id }}" @selected(old('assignee_id') == $membership->user_id)>{{ $membership->user->name ?: $membership->user->email }}</option>@endforeach</select></label>@endunless
    <div class="flex gap-3 border-t border-slate-100 pt-5"><button class="rounded-xl bg-indigo-600 px-5 py-3 text-sm font-semibold text-white">{{ $editing ? 'Save changes' : 'Create task' }}</button><a href="{{ $editing ? route('tasks.show', $task) : route('projects.show', $project) }}" class="rounded-xl px-4 py-3 text-sm font-semibold text-slate-600 hover:bg-slate-100">Cancel</a></div>
</form>
