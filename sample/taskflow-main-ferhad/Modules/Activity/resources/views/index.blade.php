@extends('layouts.app')

@section('title', 'Activity')
@section('page-title', 'Activity')

@section('content')
<section class="rounded-3xl bg-slate-950 p-7 text-white"><p class="text-sm font-semibold uppercase tracking-widest text-indigo-300">Workspace history</p><h2 class="mt-3 text-3xl font-semibold">Meaningful changes, in context.</h2></section>
<form method="GET" class="mt-7 grid gap-4 rounded-2xl border bg-white p-6 shadow-sm md:grid-cols-3"><label class="text-sm font-semibold text-slate-700">Event<select name="event" class="mt-1 w-full rounded-xl border-slate-300"><option value="">All events</option>@foreach($options['events'] as $event)<option value="{{ $event }}" @selected(($filters['event'] ?? '') === $event)>{{ \Modules\Activity\Support\ActivityDisplay::label(new \Spatie\Activitylog\Models\Activity(['event' => $event])) }}</option>@endforeach</select></label><label class="text-sm font-semibold text-slate-700">Project<select name="project" class="mt-1 w-full rounded-xl border-slate-300"><option value="">All projects</option>@foreach($options['projects'] as $project)<option value="{{ $project->id }}" @selected((string) ($filters['project'] ?? '') === (string) $project->id)>{{ $project->name }}</option>@endforeach</select></label><label class="text-sm font-semibold text-slate-700">Task<select name="task" class="mt-1 w-full rounded-xl border-slate-300"><option value="">All tasks</option>@foreach($options['tasks'] as $task)<option value="{{ $task->id }}" @selected((string) ($filters['task'] ?? '') === (string) $task->id)>{{ $task->number }} — {{ $task->title }}</option>@endforeach</select></label><label class="text-sm font-semibold text-slate-700">Actor<select name="actor" class="mt-1 w-full rounded-xl border-slate-300"><option value="">All actors</option>@foreach($options['actors'] as $actor)<option value="{{ $actor->id }}" @selected((string) ($filters['actor'] ?? '') === (string) $actor->id)>{{ $actor->name ?: $actor->email }}</option>@endforeach</select></label><label class="text-sm font-semibold text-slate-700">Date from<input type="date" name="date_from" value="{{ $filters['date_from'] ?? '' }}" class="mt-1 w-full rounded-xl border-slate-300"></label><label class="text-sm font-semibold text-slate-700">Date to<input type="date" name="date_to" value="{{ $filters['date_to'] ?? '' }}" class="mt-1 w-full rounded-xl border-slate-300"></label><div class="flex items-end gap-3"><button class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">Apply filters</button><a href="{{ route('activity.index') }}" class="rounded-xl px-4 py-2.5 text-sm font-semibold text-slate-600">Clear</a></div></form>
<section class="mt-7 rounded-2xl border bg-white shadow-sm">@forelse($activities as $activity)<article class="border-b border-slate-100 px-6 py-5"><p class="font-semibold text-slate-900">{{ \Modules\Activity\Support\ActivityDisplay::label($activity) }}</p>@if($summary = \Modules\Activity\Support\ActivityDisplay::summary($activity))<p class="mt-1 text-sm text-slate-600">{{ $summary }}</p>@endif<p class="mt-1 text-sm text-slate-500">{{ $activity->causer?->name ?: 'System' }} · {{ $activity->created_at->diffForHumans() }}</p></article>@empty<div class="px-6 py-14 text-center text-sm text-slate-500">No activity is available in your workspace yet.</div>@endforelse<div class="px-6 py-4">{{ $activities->links() }}</div></section>
@endsection
