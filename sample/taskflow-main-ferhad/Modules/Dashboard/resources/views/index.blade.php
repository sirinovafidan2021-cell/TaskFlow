@extends('layouts.app')

@section('title', 'Dashboard')
@section('page-title', 'Dashboard')

@section('content')
<section class="rounded-3xl bg-slate-950 p-7 text-white shadow-xl shadow-slate-900/10"><p class="text-sm font-semibold uppercase tracking-widest text-indigo-300">Live overview</p><h2 class="mt-3 text-3xl font-semibold">Work that needs your attention.</h2></section>
<section class="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">@foreach(['Active Projects' => $activeProjects, 'Archived Projects' => $archivedProjects, 'Total Tasks' => $totalTasks, 'Todo' => $todo, 'In Progress' => $inProgress, 'Review' => $review, 'Overdue' => $overdue, 'Completed Today' => $completedToday] as $label => $value)<article class="rounded-2xl border bg-white p-5 shadow-sm"><p class="text-xs font-semibold uppercase tracking-wide text-slate-400">{{ $label }}</p><p class="mt-3 text-3xl font-semibold text-slate-950">{{ $value }}</p></article>@endforeach</section>
<section class="mt-7 grid gap-6 lg:grid-cols-2"><article class="rounded-2xl border bg-white p-6 shadow-sm"><h3 class="font-semibold">My Tasks</h3><div class="mt-4 space-y-3">@forelse($myTasks as $task)<a href="{{ route('tasks.show', $task) }}" class="block rounded-xl bg-slate-50 p-4 transition hover:bg-indigo-50"><p class="font-semibold text-slate-900">{{ $task->number }} · {{ $task->title }}</p><p class="mt-1 text-sm text-slate-500">{{ $task->project->name }} · Due {{ $task->due_at?->format('M j') ?? 'not set' }}</p></a>@empty<p class="text-sm text-slate-500">No assigned tasks right now.</p>@endforelse</div></article><article class="rounded-2xl border bg-white p-6 shadow-sm"><h3 class="font-semibold">Recent Activity</h3><div class="mt-4 space-y-3">@forelse($recentActivity as $activity)<div class="text-sm"><p class="font-semibold text-slate-900">{{ \Modules\Activity\Support\ActivityDisplay::label($activity) }}</p>@if($summary = \Modules\Activity\Support\ActivityDisplay::summary($activity))<p class="text-slate-600">{{ $summary }}</p>@endif<p class="text-slate-500">{{ $activity->causer?->name ?: 'System' }} · {{ $activity->created_at->diffForHumans() }}</p></div>@empty<p class="text-sm text-slate-500">No recent activity in your permitted workspace.</p>@endforelse</div></article></section>
<section class="mt-7 rounded-2xl border bg-white p-6 shadow-sm"><div class="flex items-center justify-between"><h3 class="font-semibold">Task Status Overview</h3><p class="text-sm text-slate-500">{{ $totalTasks }} visible tasks</p></div><div class="mt-5 grid gap-4 sm:grid-cols-3">@foreach(['Todo' => $todo, 'In Progress' => $inProgress, 'Review' => $review] as $label => $value)<div><div class="flex justify-between text-sm"><span class="font-medium text-slate-700">{{ $label }}</span><span class="text-slate-500">{{ $value }}</span></div><div class="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div class="h-full rounded-full bg-indigo-500" style="width: {{ $totalTasks ? min(100, ($value / $totalTasks) * 100) : 0 }}%"></div></div></div>@endforeach</div></section>
@endsection
