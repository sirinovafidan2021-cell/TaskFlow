@extends('layouts.app')
@section('title', 'Notifications') @section('page-title', 'Notifications')
@section('content')<div class="mx-auto max-w-4xl"><form method="POST" action="{{ route('notifications.read-all') }}">@csrf @method('PATCH')<button class="rounded-xl border border-slate-300 px-4 py-2 text-sm font-semibold">Mark all read</button></form><div class="mt-5 space-y-3">@forelse($notifications as $notification)<article class="rounded-2xl border border-slate-200 bg-white p-5 {{ $notification->read_at ? '' : 'ring-1 ring-indigo-200' }}"><div class="flex justify-between gap-3"><div><p class="font-semibold">{{ $notification->data['task_number'] ?? 'Task update' }} · {{ $notification->data['task_title'] ?? '' }}</p><a class="mt-2 inline-block text-sm font-semibold text-indigo-700" href="{{ route('tasks.show', $notification->data['task_id']) }}">Open task</a></div>@if(!$notification->read_at)<form method="POST" action="{{ route('notifications.read', $notification) }}">@csrf @method('PATCH')<button class="text-sm font-semibold text-slate-600">Mark read</button></form>@endif</div></article>@empty<p class="rounded-2xl border border-dashed border-slate-300 p-8 text-center text-sm text-slate-500">No notifications.</p>@endforelse</div><div class="mt-5">{{ $notifications->links() }}</div></div>@endsection
