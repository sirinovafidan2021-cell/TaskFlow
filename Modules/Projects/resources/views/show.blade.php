@extends('layouts.app')

@section('title', $project->name)
@section('page-title', 'Project details')

@section('content')
<div class="mx-auto max-w-5xl"><section class="rounded-3xl bg-slate-950 p-7 text-white shadow-xl shadow-slate-900/10"><a href="{{ route('projects.index') }}" class="text-sm font-semibold text-indigo-300 hover:text-white">← All projects</a><div class="mt-5 flex flex-wrap items-center gap-3"><h2 class="text-3xl font-semibold">{{ $project->name }}</h2><span class="rounded-full bg-white/10 px-3 py-1 text-xs font-semibold">{{ ucfirst($project->status->value) }}</span></div><p class="mt-3 max-w-2xl text-sm leading-7 text-slate-300">{{ $project->description ?: 'No description has been added yet.' }}</p><div class="mt-6 flex flex-wrap gap-2">@can('update', $project)<a href="{{ route('projects.edit', $project) }}" class="rounded-xl bg-white px-4 py-2.5 text-sm font-semibold text-slate-950">Edit</a>@if($project->status === \Modules\Projects\Enums\ProjectStatus::Draft)<form method="POST" action="{{ route('projects.activate', $project) }}">@csrf @method('PATCH')<button class="rounded-xl bg-emerald-400 px-4 py-2.5 text-sm font-semibold text-emerald-950">Activate project</button></form>@endif @endcan @can('archive', $project) @if($project->status !== \Modules\Projects\Enums\ProjectStatus::Archived)<form method="POST" action="{{ route('projects.archive', $project) }}">@csrf @method('PATCH')<button class="rounded-xl border border-amber-300/40 px-4 py-2.5 text-sm font-semibold text-amber-100">Archive</button></form>@endif @endcan @can('manageMembers', $project)<a href="{{ route('projects.members.index', $project) }}" class="rounded-xl border border-white/20 px-4 py-2.5 text-sm font-semibold text-white">Manage members</a>@endcan</div></section><section class="mt-7 grid gap-5 sm:grid-cols-2 lg:grid-cols-4"><article class="rounded-2xl border bg-white p-5"><p class="text-xs uppercase text-slate-400">Owner</p><p class="mt-3 font-semibold">{{ $project->owner->name ?: $project->owner->email }}</p></article><article class="rounded-2xl border bg-white p-5"><p class="text-xs uppercase text-slate-400">Members</p><p class="mt-3 font-semibold">{{ $project->memberships_count }} people</p></article><article class="rounded-2xl border bg-white p-5"><p class="text-xs uppercase text-slate-400">Start date</p><p class="mt-3 font-semibold">{{ $project->starts_at?->format('M j, Y') ?? 'Not set' }}</p></article><article class="rounded-2xl border bg-white p-5"><p class="text-xs uppercase text-slate-400">Due date</p><p class="mt-3 font-semibold">{{ $project->due_at?->format('M j, Y') ?? 'Not set' }}</p></article></section><section class="mt-7 rounded-2xl border bg-white p-6 shadow-sm"><div class="flex flex-wrap items-center justify-between gap-4"><div><h3 class="text-lg font-semibold">Tasks</h3><p class="mt-1 text-sm text-slate-500">{{ $project->status === \Modules\Projects\Enums\ProjectStatus::Active ? 'This project is active and ready for focused work.' : 'Activate this project before creating tasks.' }}</p></div><div class="flex gap-3"><a href="{{ route('tasks.index', ['project_id' => $project->id]) }}" class="rounded-xl px-4 py-2.5 text-sm font-semibold text-indigo-700 hover:bg-indigo-50">View tasks</a>@can('create', [\Modules\Tasks\Models\Task::class, $project]) @if($project->status === \Modules\Projects\Enums\ProjectStatus::Active)<a href="{{ route('tasks.create', $project) }}" class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">New task</a>@endif @endcan</div></div></section><section class="mt-7 rounded-2xl border bg-white p-6 shadow-sm"><div class="flex items-center justify-between"><h3 class="font-semibold">Recent activity</h3>@if($canViewActivity)<a href="{{ route('activity.index', ['project' => $project->id]) }}" class="text-sm font-semibold text-indigo-600">View all activity</a>@endif</div><div class="mt-4 space-y-3">@forelse($activities as $activity)<div class="text-sm"><p class="font-semibold text-slate-900">{{ \Modules\Activity\Support\ActivityDisplay::label($activity) }}</p>@if($summary = \Modules\Activity\Support\ActivityDisplay::summary($activity))<p class="text-slate-600">{{ $summary }}</p>@endif<p class="text-slate-500">{{ $activity->causer?->name ?: 'System' }} · {{ $activity->created_at->diffForHumans() }}</p></div>@empty<p class="text-sm text-slate-500">No activity for this project yet.</p>@endforelse</div></section></div>
@endsection
