@extends('layouts.app')
@section('title', 'Backlog') @section('page-title', 'Backlog')
@section('content')<div class="mx-auto max-w-5xl"><a class="text-sm font-semibold text-indigo-700" href="{{ route('projects.show',$project) }}">← {{ $project->name }}</a><h2 class="mt-3 text-3xl font-semibold">Project backlog</h2><div class="mt-6 space-y-3">@forelse($tasks as $task)<article class="rounded-2xl border border-slate-200 bg-white p-5"><div class="flex justify-between"><a class="font-semibold text-indigo-700" href="{{ route('tasks.show',$task) }}">{{ $task->number }} · {{ $task->title }}</a><span class="text-sm text-slate-500">{{ $task->status->value }} · #{{ $task->rank }}</span></div></article>@empty<p class="text-slate-500">No work items.</p>@endforelse</div><div class="mt-5">{{ $tasks->links() }}</div></div>@endsection
