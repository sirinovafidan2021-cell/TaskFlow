@extends('layouts.app')
@section('title', 'Tasks') @section('page-title', 'Tasks')
@section('content')
<section class="rounded-3xl bg-slate-950 p-7 text-white shadow-xl shadow-slate-900/10"><p class="text-sm font-semibold uppercase tracking-widest text-indigo-300">Task workspace</p><h2 class="mt-3 text-3xl font-semibold">Make project work visible and actionable.</h2><p class="mt-2 text-sm text-slate-300">Filter priorities, delivery dates, ownership, and status from one focused view.</p></section>
<form method="GET" class="mt-7 grid gap-3 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm md:grid-cols-3 xl:grid-cols-4"><input name="q" value="{{ request('q') }}" placeholder="Search number or title" class="rounded-xl border border-slate-300 px-3 py-2.5 text-sm"><select name="status" class="rounded-xl border border-slate-300 px-3 py-2.5 text-sm"><option value="">All statuses</option>@foreach($statuses as $status)<option value="{{ $status->value }}" @selected(request('status')===$status->value)>{{ ucfirst(str_replace('_',' ',$status->value)) }}</option>@endforeach</select><select name="priority" class="rounded-xl border border-slate-300 px-3 py-2.5 text-sm"><option value="">All priorities</option>@foreach($priorities as $priority)<option value="{{ $priority->value }}" @selected(request('priority')===$priority->value)>{{ ucfirst($priority->value) }}</option>@endforeach</select><select name="project_id" class="rounded-xl border border-slate-300 px-3 py-2.5 text-sm"><option value="">All projects</option>@foreach($projects as $project)<option value="{{ $project->id }}" @selected(request('project_id')==$project->id)>{{ $project->name }}</option>@endforeach</select><select name="assignee_id" class="rounded-xl border border-slate-300 px-3 py-2.5 text-sm"><option value="">All assignees</option>@foreach($users as $user)<option value="{{ $user->id }}" @selected(request('assignee_id')==$user->id)>{{ $user->name ?: $user->email }}</option>@endforeach</select><input type="date" name="due_before" value="{{ request('due_before') }}" class="rounded-xl border border-slate-300 px-3 py-2.5 text-sm"><select name="sort" class="rounded-xl border border-slate-300 px-3 py-2.5 text-sm">@foreach(['created_at'=>'Newest','due_at'=>'Due date','priority'=>'Priority','status'=>'Status','number'=>'Number'] as $key=>$label)<option value="{{ $key }}" @selected(request('sort','created_at')===$key)>{{ $label }}</option>@endforeach</select><select name="direction" class="rounded-xl border border-slate-300 px-3 py-2.5 text-sm"><option value="desc">Descending</option><option value="asc" @selected(request('direction')==='asc')>Ascending</option></select><button class="rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white">Apply filters</button></form>
<section class="mt-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm">@if($tasks->isEmpty())<div class="px-6 py-14 text-center"><p class="text-lg font-semibold">No tasks match this view.</p><p class="mt-2 text-sm text-slate-500">Activate a project to begin creating focused work.</p></div>@else<div class="overflow-x-auto"><table class="min-w-full divide-y divide-slate-100 text-sm"><thead class="bg-slate-50 text-left text-xs uppercase tracking-wide text-slate-500"><tr><th class="px-5 py-4">Task</th><th class="px-5 py-4">Project</th><th class="px-5 py-4">Assignee</th><th class="px-5 py-4">Status</th><th class="px-5 py-4">Priority</th><th class="px-5 py-4">Due</th></tr></thead><tbody class="divide-y divide-slate-100">@foreach($tasks as $task)<tr><td class="px-5 py-4"><a class="font-semibold text-indigo-700" href="{{ route('tasks.show',$task) }}">{{ $task->number }}</a><p class="mt-1 font-medium text-slate-900">{{ $task->title }}</p></td><td class="px-5 py-4 text-slate-600">{{ $task->project->name }}</td><td class="px-5 py-4 text-slate-600">{{ $task->assignee?->name ?: $task->assignee?->email ?: 'Unassigned' }}</td><td class="px-5 py-4"><span class="rounded-full bg-sky-50 px-2.5 py-1 text-xs font-semibold text-sky-700">{{ ucfirst(str_replace('_',' ',$task->status->value)) }}</span></td><td class="px-5 py-4"><span class="rounded-full bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-700">{{ ucfirst($task->priority->value) }}</span></td><td class="px-5 py-4 text-slate-500">{{ $task->due_at?->format('M j, Y') ?? '—' }}</td></tr>@endforeach</tbody></table></div><div class="border-t border-slate-100 px-5 py-4">{{ $tasks->links() }}</div>@endif</section>
@endsection
